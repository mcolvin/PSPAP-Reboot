getwd()
setwd("C:/Users/sreynolds/Documents/GitHub/PSPAP-Reboot/00-reference-population")
dens<-read.csv("./dat/fish_density.csv")
head(dens)
?ddply
??ddply
dens<-read.csv("./dat/fish_density.csv")
ddply(bends,.(basin,b_segment),summarize)
library(reshape2)
library(plyr)
library(knitr)
library(pander)
ddply(bends,.(basin,b_segment),summarize)
ddply(bends,.(basin,b_segment),summarize,
n_bends=length(id),
min_rkm=min(length.rkm),
mean_rkm=min(length.rkm),
maximum_rkm=min(length.rkm))
format(tmp,digits=2)
tmp<-ddply(bends,.(basin,b_segment),summarize,
n_bends=length(id),
min_rkm=min(length.rkm),
mean_rkm=min(length.rkm),
maximum_rkm=min(length.rkm))
format(tmp,digits=2)
rm(tmp)
tmp<-ddply(bends,.(basin,b_segment),summarize,
n_bends=length(id),
min_rkm=min(length.rkm))
tmp
tmp<-ddply(bends,.(basin,b_segment),summarize,
n_bends=length(id))
tmp
tmp<-ddply(bends,.(basin,b_segment),summarize)
rm(tmp)
head(dens)
tmp<-ddply(dens,.(RPMA,segment),summarize,
min_dens=min(d_lower))
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower))
tmp
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower,density_mean))
tmp
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(c(d_lower,density_mean))
tmp
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(c(d_lower),)
m)
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(c(d_lower),
min_mean)
)
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower),
min_mean=min(density_mean),
max_mean=max(density_mean),
max_dens=max(d_upper))
tmp
?summarize
?min
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower, na.rm=TRUE),
min_mean=min(density_mean, na.rm=TRUE),
max_mean=max(density_mean, na.rm=TRUE),
max_dens=max(d_upper, na.rm=TRUE))
tmp
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower),
min_mean=min(density_mean, na.rm=TRUE),
max_mean=max(density_mean, na.rm=TRUE),
max_dens=max(d_upper))
tmp
tmp$min_dens<-min(min_dens,min_mean)
tmp$min_dens<-min(tmp$min_dens,tmp$min_mean)
tmp
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower),
min_mean=min(density_mean, na.rm=TRUE),
max_mean=max(density_mean, na.rm=TRUE),
max_dens=max(d_upper))
tmp$min_dens<-min(tmp$min_dens,tmp$min_mean, na.rm=TRUE)
tmp
tmp<-ddply(dens,.(RPMA,Segments),summarize,
min_dens=min(d_lower),
min_mean=min(density_mean, na.rm=TRUE),
max_mean=max(density_mean, na.rm=TRUE),
max_dens=max(d_upper))
tmp
